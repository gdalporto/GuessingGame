import React from 'react';
import { shallow } from 'enzyme';
import What from './what';

describe('<What /> component testing',()=>{
    let winningNumber=50;

    // smoke test
    it('should render',()=>{
        shallow(<What />);
    })

    // props and state
        //N/A
    // callbacks and events
        // N/A

})
